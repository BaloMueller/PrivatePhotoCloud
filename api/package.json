{
  "name": "PrivatePhotoCloud-api",
  "version": "1.0.0",
  "description": "All dynamic functions to access and modify the photos on the storage repository.",
  "author": "Sebastian Müller <s@mlr.digital>",
  "license": "GPL-3.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "yarn run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/storage-blob": "^12.2.0-preview.1",
    "jimp": "^0.16.0"
  },
  "devDependencies": {
    "@azure/functions": "^1.2.2",
    "typescript": "^3.3.3"
  }
}
